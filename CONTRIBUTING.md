# 贡献指南

感谢你对 EikoCode 的兴趣！这是一个个人风格很强的项目，贡献前请先了解它的脾气。

## 改动前必须知道的原则

- **可预测优先于智能**：上下文只由用户显式改变，工具不做任何静默的摘要 / 截断 / 丢弃。任何"自动帮忙"的行为都要三思。
- **不静默降级**：功能不可用就明确报错并说明原因（含服务名 / 退出码），不要悄悄换成另一条路。
- **失败可见**：错误归一为固定类别，给用户能读懂的文案，不抛堆栈。

如果你的想法和这些原则冲突，先开 Issue 讨论，不要直接动手。

## 提交改动

```powershell
# 开发环境
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"

# 跑测试（离线，全部用假响应，不联网、不花 API 钱）
pytest
```

要求：

1. **所有测试离线可跑**——不要引入依赖真实 API 的测试
2. 改了行为就同步更新 README 对应章节，命令、默认值、错误文案以实际实现为准
3. 只支持 Windows：涉及终端 / 编码 / 换行符时，注意 GBK 代码页、CRLF / LF、PowerShell 5.1 无 BOM 读文件等坑
4. 一个 PR 一个主题，别顺手夹带格式化全文件

## 提 Issue

报 bug 请带上：Windows 版本、Python 版本、终端（Windows Terminal / conhost）、复现步骤、完整报错文案。功能建议请先说清楚它和「可预测优先于智能」原则的关系。
